# Scroll Keeper Platform-Agnostic Configuration
# This configuration file works across any hosting environment

# Core Configuration
PORT=5000
NODE_ENV=production

# API Paths - Customize as needed for your environment
API_PATH=/api
WEBSOCKET_PATH=/ws

# Vector Search Configuration
VECTOR_DIMENSION=384
VECTOR_METRIC=cosine
VECTOR_SEARCH_LIMIT=10

# LLM Service Configuration
LLM_PROVIDER=huggingface
LLM_MODEL=sentence-transformers/all-MiniLM-L6-v2
# Uncomment and set your API key
# HUGGINGFACE_API_KEY=

# Fallback LLM Configuration
FALLBACK_LLM_ENABLED=true
FALLBACK_LLM_PROVIDER=local
FALLBACK_LLM_MODEL=mini

# Extraction Configuration
EXTRACTION_TIMEOUT=60000
EXTRACTION_MAX_RETRIES=3
EXTRACTION_BATCH_SIZE=10

# Processing Configuration
PROCESSING_THREADS=2
PROCESSING_QUEUE_SIZE=100
PROCESSING_INTERVAL=5000

# Storage Configuration
STORAGE_TYPE=memory
# Uncomment for persistent storage
# STORAGE_TYPE=persistent
# STORAGE_PATH=/path/to/storage

# Performance Optimizations
ENABLE_COMPRESSION=true
ENABLE_MEMORY_OPTIMIZATION=false
ENABLE_VECTOR_CLUSTERING=false
ENABLE_PARALLEL_PROCESSING=false
ENABLE_SHARDED_STORAGE=false
ENABLE_ENCRYPTED_STORAGE=false

# Logging Configuration
LOG_LEVEL=info
LOG_FORMAT=json
LOG_TO_FILE=false
# LOG_FILE_PATH=/path/to/logs

# Security Configuration
CORS_ENABLED=true
CORS_ORIGINS=*
# For production, specify allowed origins
# CORS_ORIGINS=https://yourdomain.com,https://api.yourdomain.com

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Advanced Configuration
NODE_OPTIONS=--max-old-space-size=512